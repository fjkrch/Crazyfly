# Results status

This is an engineering validation report, not a completed biological-circuit comparison or evidence of learned locomotion. The latest full-budget result and queue status appear first; earlier sections preserve the state at the time of each check.

## First two full-budget main-matrix seeds — 2026-09-14

The [heldout_v3 queue](../runs/main_matrix_malecns_v1_heldout_v3_20260913.json) has passed `frozen_lif_original` seeds 0 and 1 and is paused after seed 2 stopped at 6,618 logged updates without a checkpoint; 17 jobs remain untouched and ready. The first job saved a [full-budget checkpoint](../runs/main_matrix_malecns_v1_heldout_v3_20260913/training/20260913T150442Z/FlyG1-GoalReach-FreePosture-v0/frozen_lif/seed-0/checkpoint.pt) after **5,000,192 actual environment interactions** (9,766 updates; 5,000,000 requested). Its three fresh-process, 16-episode `heldout_v1` evaluations completed: [GoalReach](../runs/main_matrix_malecns_v1_heldout_v3_20260913/evaluations/frozen_lif_original__seed-0/FlyG1-GoalReach-FreePosture-v0.json), [GoalSwitch](../runs/main_matrix_malecns_v1_heldout_v3_20260913/evaluations/frozen_lif_original__seed-0/FlyG1-GoalSwitch-FreePosture-v0.json), and [PushRecovery](../runs/main_matrix_malecns_v1_heldout_v3_20260913/evaluations/frozen_lif_original__seed-0/FlyG1-PushRecovery-FreePosture-v0.json). Target success was **0/16 in each scenario**. GoalSwitch recorded three target changes per episode; PushRecovery recorded three pushes (60 N·s summed impulse magnitude) per episode and **0/48 successful recoveries** under the declared strict rule: a sustained target success within three seconds after a push. This does not measure post-push mobility or posture recovery. The [source-backed partial comparison](../runs/main_matrix_malecns_v1_heldout_v3_20260913_comparison.json) and [Markdown table](../runs/main_matrix_malecns_v1_heldout_v3_20260913_comparison.md) validate **2/20 jobs with no validation errors**. Between-condition pairing remains untested because only the original-LIF condition has completed jobs. These results prove the full-budget train/checkpoint/evaluate path for two independent seeds, but do not show useful learning or comparative efficacy.

Seed 1 saved its [full-budget checkpoint](../runs/main_matrix_malecns_v1_heldout_v3_20260913/training/20260913T180744Z/FlyG1-GoalReach-FreePosture-v0/frozen_lif/seed-1/checkpoint.pt) after 5,000,192 actual interactions and completed three fresh-process, 16-episode evaluations: [GoalReach](../runs/main_matrix_malecns_v1_heldout_v3_20260913/evaluations/frozen_lif_original__seed-1/FlyG1-GoalReach-FreePosture-v0.json), [GoalSwitch](../runs/main_matrix_malecns_v1_heldout_v3_20260913/evaluations/frozen_lif_original__seed-1/FlyG1-GoalSwitch-FreePosture-v0.json), and [PushRecovery](../runs/main_matrix_malecns_v1_heldout_v3_20260913/evaluations/frozen_lif_original__seed-1/FlyG1-PushRecovery-FreePosture-v0.json). It also recorded 0/16 target successes in each scenario and 0/48 strict push recoveries. Mean goal-relative progress was +0.255 m, +0.260 m, and +0.232 m respectively, versus negative values for seed 0; two seeds are too few to infer a reliable performance difference. Its checkpoint loads on CPU, and all 9,766 update records are ordered and finite. The 0.05 KL guard rejected an optimizer attempt in 1,622 updates, including 579 with no accepted epoch; maximum accepted KL was 0.04998. Seed 1 trained for about 2.96 hours, with sampled maxima of 2,812/8,151 MiB device-wide GPU, 4,065.05 MiB process RSS, and 44.0% system RAM. Both seeds used matching schedules, episode plans, and initial states in each scenario.

The first run's training manifest contains 9,766 contiguous, finite update records. The 0.05 post-step KL guard rejected an optimizer attempt in 2,602 updates, including 1,048 updates with no accepted epoch; maximum accepted KL was 0.04998. It reports 42,767 trainable and 5,103 frozen parameters, about 3.02 hours of training wall time, and sampled maxima of 2,811/8,151 MiB device-wide GPU, 4,072.52 MiB process RSS, and 44.8% system RAM. These memory values are samples, not a guaranteed whole-run peak or a benchmark for the target Windows machine. The guarded handoff for companion posture/contact replay was stopped with the main run and must be rearmed after restart; its 60 replays have not yet run.

## Deterministic LIF replay and current queue — 2026-09-13

For the present 256-neuron subset, the frozen LIF core now derives a deterministic dense recurrent matrix to avoid CUDA edge-accumulation order changes near hard spike thresholds. Frozen-LIF PPO rejects circuits above 1,024 neurons until a deterministic sparse replay path is validated. Dense-versus-edge current and input-gradient tests pass; the full CPU unit suite now passes **104/104 tests**. The report test covers four conditions, five seeds, three scenarios, and 16 distinct episodes per scenario; it rejects a corrupted episode or seed mean. Read-only monitor tests cover partial logs, stale process evidence, evaluation-stage logs, and checkpoint files awaiting runner verification. The guarded post-main handoff tests cover incomplete/stale reports and an active simulator worker.

A fresh 16-environment [60-update training manifest](../runs/train-replay-dense-v5/20260913T145118Z/FlyG1-GoalReach-FreePosture-v0/frozen_lif/seed-0/manifest.json) records **30,720 environment interactions** and a saved [checkpoint](../runs/train-replay-dense-v5/20260913T145118Z/FlyG1-GoalReach-FreePosture-v0/frozen_lif/seed-0/checkpoint.pt). The run crossed the 1,000-control-step timeout/reset boundary and continued recurrent PPO replay without the former mismatch. It accepted 104 PPO epochs and rejected 12 update attempts under the 0.05 post-step KL guard; maximum accepted KL was 0.04869. Stage memory samples were 2,822/8,151 MiB device-wide GPU at the first and last optimizer stages, 41.2% then 40.8% system RAM, and 31.13 then 35.56 MiB sampled PyTorch peak allocation. These stage readings do not establish whole-run peak memory or full-budget stability.

Fresh-process, one-episode `heldout_v1` evaluations from that checkpoint completed on [GoalReach](../runs/train-replay-dense-v5/evaluation_heldout_v1/GoalReach.json), [GoalSwitch](../runs/train-replay-dense-v5/evaluation_heldout_v1/GoalSwitch.json), and [PushRecovery](../runs/train-replay-dense-v5/evaluation_heldout_v1/PushRecovery.json). Each had 0/1 sustained successes; GoalSwitch logged three timed target changes, and PushRecovery logged three pushes totaling 60 N·s in impulse magnitude and 0/3 recovery successes. The default standing reset remains the only evaluated reset regime. One training seed and one episode per scenario do not establish comparative performance or useful learning.

The new [heldout_v3 main queue](../runs/main_matrix_malecns_v1_heldout_v3_20260913.json) and [summary](../runs/main_matrix_malecns_v1_heldout_v3_20260913_summary.json) are **paused: original-LIF seeds 0 and 1 passed, seed 2 stopped without a checkpoint, and 17 jobs remain ready**, four conditions × five seeds at 5,000,000 requested interactions each. The first two jobs completed full-budget checkpoints and all three 16-episode evaluations each, as reported above. The [main comparison JSON](../runs/main_matrix_malecns_v1_heldout_v3_20260913_comparison.json) is **incomplete: 2/20 jobs validated**, with no validation errors; 18 jobs remain unfinished. The saved queue JSON still labels seed 2 `running`, but no matrix runner or trainer is live; resuming restarts that seed from update zero. The [integration v3 matrix](../runs/main_matrix_integration_v3_20260913.json) completed four one-update jobs at 512 interactions each and all 12 held-out evaluation JSONs. Its [comparison JSON](../runs/main_matrix_integration_v3_20260913_comparison.json) and [Markdown table](../runs/main_matrix_integration_v3_20260913_comparison.md) report complete status, four validated jobs, zero validation errors, and shared schedule, episode-plan, and initial-state hashes across conditions for all three scenarios. The integration-v3 comparison was regenerated under the latest analysis source and remains complete at 4/4, with zero validation errors. `scripts/summarize_matrix.py` rejects absent, nonfinite, or episode-inconsistent metrics while allowing documented conditional nulls, such as target time when no success occurs and recovery metrics when no recovery completes. It validates all training update rows and metadata on disk and now reports the scalar Task 6 metrics currently captured by the evaluator plus parameter counts, actual environment interactions, training wall time, per-seed training-manifest SHA-256 provenance, and sampled GPU/RAM usage. The regenerated integration report includes the resource tables; those are stage samples from one-update jobs, not full-run resource peaks. On a terminal step, Isaac auto-resets the environment before `evaluate.py` reads joint positions, applied torques, and contact forces; that step's joint-limit, saturation, and impact samples may therefore reflect the reset state. Treat those three metrics as provisional until a pre-reset measurement path is validated. Observable posture/contact regimes are absent from the evaluation JSONs and require paired trace recording. A separate `scripts/record_heldout_regimes.py` sidecar is prepared to check a completed 16-episode held-out result against its checkpoint, schedule, paired plans, initial states, outcomes, event timing, and total steps while saving bounded, named-body pre-action traces. `scripts/regime_metrics.py` computes CPU-only root orientation/height and net-force proxies by episode and training seed; `scripts/execute_regime_matrix.sh` will run 60 sequential companion replays and write source-checked comparisons after the main queue completes. Their CPU checks pass, but companion simulator replay and regime results remain untested until the main matrix completes; net force does not identify ground support or a gait. This establishes two full-budget original-LIF jobs and four one-update integration jobs; it does not establish useful learning or comparative efficacy. The heldout_v2 queue remains stale and must not be resumed.

The integration-v3 training manifests report 42,767 trainable parameters for each frozen-LIF condition, 124,975 for the GRU, and 193,583 for the MLP. The main matrix uses these same architectures and equal environment-interaction budgets, but it does not enforce a model-size-matched control or a comparable hyperparameter-search budget. Any performance difference must be interpreted with those capacity and tuning differences visible; the GRU and MLP are engineering baselines rather than size-matched neural controls.

## MaleCNS intake and frozen-LIF pilots — 2026-09-13

The user's downloaded public MaleCNS v1.0 annotations, neurotransmitter predictions, and minconf-0.5 flat connectome are now under [`data/connectome/raw/`](../data/connectome/raw/). The reproducible converter [`scripts/prepare_malecns.py`](../scripts/prepare_malecns.py) generated a [real-source manifest](../data/connectome/manifest.json), [neuron records](../data/connectome/neurons.json), [edge records](../data/connectome/edges.json), and [selection audit](../data/connectome/audit.json). The [data README](../data/connectome/README.md) records official URLs, source row counts, and local SHA-256 values. The selected leg VNC subset contains 256 neurons (24 proprioceptive sensory, eight descending, 200 interneurons, 24 motor readouts) and 5,103 directed edges. Of these edges, 3,549 receive positive ACh-model signs and 1,554 negative GABA-model signs. All 32 inputs have a selected path to at least one output; all 24 outputs are reachable. Synapse counts are retained separately from the dimensionless signed LIF weights.

The first one-update original and degree-rewired pilots at threshold 1.0 launched, but the [original-circuit trace](../runs/pilot-real-lif-original.npz) had zero spikes. An [encoder-current trace](../runs/pilot-real-lif-input-current.npz) supported calibrating the dimensionless threshold. The final real manifest uses threshold 0.047 and surrogate beta 1.0; the latter reduces the spike function's backward derivative without changing hard forward spikes. Earlier beta-10 pilots had a raw pre-clipping gradient near 10¹⁴, while beta-1 pilots had gradient norms near 1–2. A two-update pilot at the original 3e-4 rate still had large policy shifts, so the matrix config uses a 1e-6 LIF learning rate and a 0.05 post-step KL rollback guard.

Five-update, 16-environment guarded pilots completed with the final circuit: [original run manifest](../runs/pilot-real-lif-guarded-lr1e6/20260913T133816Z/FlyG1-GoalReach-FreePosture-v0/frozen_lif/seed-0/manifest.json) accepted 7/10 PPO epochs, rejecting two excessive-KL attempts; [rewired run manifest](../runs/pilot-real-lif-rewired-guarded-lr1e6/20260913T134006Z/FlyG1-GoalReach-FreePosture-v0/frozen_lif_rewired/seed-0/manifest.json) accepted 10/10. Their 100-control-step [original trace](../runs/pilot-real-lif-guarded-lr1e6-trace.npz) and [rewired trace](../runs/pilot-real-lif-rewired-guarded-lr1e6-trace.npz) recorded respectively 287 and 88 motor spikes at the sampled control substep, from 13/24 and 6/24 motor cells. Both were finite; no motor cell reached a 0.45 per-control-step spike rate. The original circuit had substantially more interneuron activity than the rewired control (2,849 versus 303 sampled spikes), an important condition difference to monitor. These short pilots verify signal and execution, not useful learning or a scientifically valid performance difference.

## Main-matrix queue and current-code checks — 2026-09-13

The [heldout_v2 matrix](../runs/main_matrix_malecns_v1_heldout_v2_20260913.json) and its [summary](../runs/main_matrix_malecns_v1_heldout_v2_20260913_summary.json) define four conditions × five seeds, each requesting 5,000,000 environment interactions. All 20 passed software prerequisites at queue creation. The first original-circuit seed then stopped after the log recorded iterations 0–15, before its full-budget checkpoint, with a stored-versus-replayed PPO policy log-probability mismatch diagnosed before update. It has no evaluation or performance result. The JSON/summary still report one running and 19 ready; those are stale on-disk states. Execution-source changes have since invalidated the queue fingerprint, so **heldout_v2 is not resumable**. The queue records circuit checksum `69362d71d0ae016c78bad67cbbd6673522adad43e2983796ff25862590c7169f`, the former execution fingerprint, low-amplitude simulator PASS report, condition-specific learning rates, 0.05 PPO KL guard, and the `heldout_v1` evaluation protocol. The earlier [pre-intake queue](../runs/main_matrix_20260913.json), [intermediate queue](../runs/main_matrix_malecns_v1_20260913.json), and [first held-out queue](../runs/main_matrix_malecns_v1_heldout_20260913.json) are also stale. This paragraph records the heldout_v2 failure as historical evidence. The validated 60-update subset replay run and paused heldout_v3 queue are reported above. The first five-million-interaction checkpoint and its evaluations now exist, but the complete main-matrix result remains pending.

The [historical integration matrix](../runs/main_matrix_integration_v2_20260913.json) completed four one-update jobs at 512 interactions each: original frozen LIF, degree-rewired frozen LIF, trainable GRU, and MLP baseline. All 12 held-out evaluator processes passed and wrote one episode plus one per-seed row. The [source-checked comparison JSON](../runs/main_matrix_integration_v2_20260913_comparison.json) and [Markdown table](../runs/main_matrix_integration_v2_20260913_comparison.md) validate those four checkpoints and results, with identical within-scenario schedule, episode-plan, and initial-state hashes across conditions. GoalSwitch produced three timed target changes per episode; PushRecovery produced three timed pushes and 60 N·s cumulative impulse magnitude per episode. This verifies the historical one-update train/checkpoint/evaluate/report path. It does not establish sustained recurrent-PPO replay correctness, learned locomotion, or comparative performance. The evaluator's previous GoalSwitch replacement failed at the first interval event because it required an argument Isaac Lab did not pass; the callback then received a default, and the full 1,000-step scenario completed. The runner rechecks artifact integrity on resume and stops a child silent for five minutes.

The [new RAM/VRAM addendum](memory_plan_source_th.md) is integrated into [plan.md](../plan.md). A two-update, 16-environment [LIF memory pilot](../runs/memory-lif-smoke/20260913T135457Z/FlyG1-GoalReach-FreePosture-v0/frozen_lif/seed-0/manifest.json) recorded 1,024 environment interactions. Device-wide `nvidia-smi` snapshots rose from 603 MiB before scene load to 2,869/8,151 MiB (35.2%) at optimizer update; system RAM reached 40.7%, process RSS 3,992.83 MiB, and sampled PyTorch CUDA peak allocation 77.12 MiB. The checkpoint and run manifest have identical seven-stage memory records. A one-update MLP memory pilot reached 2,815 MiB device-wide and 40.0% system RAM. These are short measurements on the Linux RTX 5060 laptop with the 256-neuron subset, not a benchmark of full MaleCNS or the target Windows RTX 4060. Device-wide readings are snapshots, not maxima between samples; PyTorch peaks omit Isaac and driver allocations.

An early [held-out PushRecovery pipeline check](../runs/eval-heldout-push-smoke.json) gave two episodes three logged 20 N·s torso pushes each, with no recovery successes. Its first target schedule was later replaced because 3.2–3.4 m offsets could hit the observation clip. The final `heldout_v1` [GoalReach check](../runs/eval-heldout-v1-postfix-smoke.json) sampled fixed evaluation-only target instances within the training 1–3 m radius support; schedule hashes and episode initial-state hashes are recorded. This is a separate held-out sample stream, not an out-of-distribution distance test. Crouched and prone/supine reset regimes remain unverified; the matrix configuration now declares standing only.

The final-schedule [acute ablation smoke](../runs/ablation-smoke-real-lif-final-schedule.json) executed one GoalReach episode each for sham, two named descending neurons, and two distinct size/degree-matched random groups. All four evaluator subprocesses passed and shared the same circuit, schedule hash, paired-plan hash, and initial-state hash. The target group had 0.482 m less accumulated goal-relative progress than sham in this **single** episode; the random controls differed too. This is a wiring and pairing check, not an effect estimate. The [ablation runner](../scripts/ablate.py) can repeat a declared group with more episodes and controls after a final trained checkpoint exists.

The [current-code simulator report](../runs/smoke-16x1000-low-amplitude.json) passed 16 environments × 1,000 control steps with uniformly sampled normalized actions in [-0.1, 0.1] after ten zero-action steps. It recorded 16 resets, 553.17 environment-control-steps/s, a minimum target-limit margin of 0.108 rad, and a minimum inter-root separation of 1.525 m. The report records the task/diagnostic source fingerprint and was accepted by the queue gate. A separate full-range [-1, 1] run reached step 650 and then stopped advancing during device validation; it was interrupted and is not a PASS. Full-range random-action stability remains unresolved.

Two earlier 16-environment, two-update PPO checks completed with finite metrics: [MLP checkpoint](../runs/pilot-current/20260913T130507Z/FlyG1-GoalReach-FreePosture-v0/mlp/seed-0/checkpoint.pt) and [GRU checkpoint](../runs/pilot-current/20260913T130532Z/FlyG1-GoalReach-FreePosture-v0/gru/seed-0/checkpoint.pt). These verified retraining under the rollout-continuation fix at that time, not useful learning. The evaluator completed four deterministic episodes from that MLP training seed in each scenario: [GoalReach](../runs/pilot-current-eval-goalreach.json) had 1/4 sustained successes and 0.376 m mean XY progress; [GoalSwitch](../runs/pilot-current-eval-goalswitch.json) logged three goal changes per episode and omits cross-target initial-to-final progress; [PushRecovery](../runs/pilot-current-eval-pushrecovery.json) logged three pushes per episode and 60 N·s cumulative impulse magnitude. A four-episode [GRU GoalReach evaluation](../runs/pilot-current-eval-gru-goalreach.json) recorded 0/4 successes and 0.426 m mean XY progress. These are small pipeline checks, with default resets and unverified held-out target and paired-disturbance protocols. Recovery success/time remain unmeasured. The unit suite passed 50 tests at the later matrix-integration check; it must be rerun after execution-code changes.

Earlier engineering evidence on 2026-09-13:

- 12 CPU unit tests passed at the earlier handoff, including circuit ingestion, frozen LIF gradients and PPO replay, reset isolation, bounded actions, sustained goal success, and LIF/GRU state continuation across PPO update boundaries;
- [asset_audit.json](asset_audit.json) resolved 37 G1 joints, 23 controlled joints, 44 bodies, and all corresponding limits, gains, and body masses;
- 1-environment and 16-environment headless runs each completed 1,000 control steps with bounded random actions and finite robot state before the latest diagnostic checks. Measured throughputs were 40.58 and 660.16 environment-control-steps/s after the normalized-observation revision. A resumed 16-environment, 1,000-step check initialized all 16 G1 instances but produced no completed rollout after five minutes and was stopped cleanly; it is not a PASS result. The smoke script now accumulates all per-step target-limit, finite-state, and root-separation checks on the GPU and synchronizes only after the rollout, so a future completed measurement does not distort throughput with repeated host/device synchronization;
- a 10-update MLP pilot with 16 environments and horizon 32 completed with finite updates and saved [checkpoint.pt](../runs/20260913T100028Z/FlyG1-GoalReach-FreePosture-v0/mlp/seed-0/checkpoint.pt). A one-update GRU pilot also completed;
- the MLP pilot was evaluated on four 20 s episodes using evaluation seed 101. The resulting [evaluation JSON](../runs/evaluation-mlp-final-seed101.json) records 0/4 sustained successes and mean final XY progress of 0.353 m. These four episodes all share one training seed, so they do not establish a reliable success rate;
- GoalSwitch changed the target once during a 300-step test, and PushRecovery applied one 200 N, 0.10 s timed torso force during a 260-step test;
- a 10-control-step [trace](../runs/recording-smoke.npz) and [MP4](../runs/recording-smoke.mp4) were saved with matching sample/frame counts (10 each). This validates the recording interface but is not an episode-level biological-circuit trace;
- a frozen-LIF launch with the example manifest stopped before Isaac Sim started and identified the missing neuron file.

The PPO collector was corrected after the recorded MLP and GRU pilots: environments and recurrent state now continue across update boundaries. CPU continuation/replay checks pass, but those older pilots do not validate simulator training under this correction.

Still pending after the current continuation:

- GUI-visible scene, contact/penetration checks, and smaller-physics-timestep stability tests (23 named target-mapping probes have passed);
- full-budget LIF and multi-seed stability, a useful-learning pilot, and the main v3 comparison; the 60-update deterministic-subset replay and four-condition one-update integration v3 have passed their narrower gates;
- full-range bounded-random-action stability, contact/penetration checks, and the smaller-timestep validation remain open despite the passing low-amplitude simulator gate;
- a useful-learning pilot with predeclared larger budget, multi-seed comparative matrix, held-out reset/disturbance evaluation, synchronized neural/video recordings, and controlled ablation results.
